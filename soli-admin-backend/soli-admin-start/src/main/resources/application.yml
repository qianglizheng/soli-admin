spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/soli-admin?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

logging:
  level:
    org.springframework.security: trace